pipeline{
    agent any
    environment{
        PATH = "$PATH:/opt/apache-maven-3.8.2/bin"
    }
    stages{
        stage('Scan'){
            steps{
                withSonarQubeEnv(installationName: 'sonartest'){
                    sh "mvn sonar::sonar"
                }
            }
        }
    }
}